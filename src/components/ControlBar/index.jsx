import React from 'react';

import { Container } from './styles';

/**
 * Rendered on Game page.
 * @returns {JSX.Element} Horizontal bar with game controls.
 */
function ControlBar() {
  return (
    <Container>
      Horizontal control bar
    </Container>
  );
}

export default ControlBar;